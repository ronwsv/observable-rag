# Normalize line endings to LF in the repo (consistent, clean diffs cross-platform).
* text=auto eol=lf

# Binary artifacts should never be normalized.
*.npy binary
*.faiss binary
*.png binary
*.jpg binary
